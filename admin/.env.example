# Admin panel local build/runtime
# Production builds must pass an explicit public API URL such as https://api.phuongtungthien.com.
VITE_API_URL=http://localhost:3001

# Optional base path when serving admin under a subpath
VITE_BASE_PATH=/
